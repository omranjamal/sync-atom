import { assert } from "chai";
import { atom } from "../src/index.mjs";

describe("atom(initialState)", () => {
  describe("this project has not been tested", () => {
    it("always passes the tests", () => {
      assert.isTrue(true);
    });
  });
});
